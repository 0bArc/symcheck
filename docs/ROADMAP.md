# Roadmap

## Shipped

1. PE/COFF parse, MSVC demangle, inspect/exports/imports/compare/explain
2. Project scan, `find` / `why`, deps, duplicates, matrix
3. PDB link + dbghelp, ELF64 symbols, `trace` / `abi` / `snapshot`
4. Build discovery, `ci`, SARIF, IDE task examples
5. `security`, `symbolize`, DWARF `.debug_line`

## Non-goals

AI in the core engine, auto-editing build files, decompiler, package manager.
